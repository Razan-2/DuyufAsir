[project]
name = "wakla"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Razan-2", email = "razanalqobti@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.141.1",
    "httpx>=0.28.1",
    "pydantic[email]>=2.13.4",
    "python-dotenv>=1.2.3",
    "uvicorn>=0.52.4",
]

[project.scripts]
wakla = "wakla:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
